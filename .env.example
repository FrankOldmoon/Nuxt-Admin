# Nuxt
# NUXT_PUBLIC_SITE_URL=https://example.com

# Dev server
# NUXT_HOST=0.0.0.0
# NUXT_PORT=3000

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/nuxt_ai
# In production, set to `true` only if the app should apply migrations on boot.
# Leave unset and run `pnpm db:migrate` in your deploy pipeline for safer releases.
# AUTO_MIGRATE=true
# Connection pool tuning (optional, defaults shown):
# DB_POOL_MAX=10
# DB_POOL_IDLE_TIMEOUT=30000
# DB_POOL_CONNECT_TIMEOUT=2000

# Session
# Generate a long random string, e.g. `openssl rand -hex 32`
SESSION_SECRET=change_me_to_a_long_random_secret_value
